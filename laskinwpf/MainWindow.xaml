<Window x:Class="WpfCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculator" Height="350" Width="400">
    <Grid Background="LightBlue">
        <!-- Lisää taustaväri tähän -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="textBoxResult" Grid.Row="0" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Center" IsReadOnly="True"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Summa" Click="ButtonSum_Click" Margin="5"/>
            <Button Content="Erotus" Click="ButtonSubtract_Click" Margin="5"/>
            <Button Content="Jako" Click="ButtonDivide_Click" Margin="5"/>
            <Button Content="Kerto" Click="ButtonMultiply_Click" Margin="5"/>
        </StackPanel>
        <TextBox x:Name="textBoxNumber" Grid.Row="2" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
        <Button Content="Nollaa" Click="ButtonReset_Click" Grid.Row="3" Margin="5"/>
    </Grid>
</Window>
